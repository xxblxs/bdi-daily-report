# NAVGreen 日报推送脚本依赖
# 运行: pip install -r requirements.txt

# 核心必需
requests>=2.31.0

# HTML 模板引擎
jinja2>=3.1.0

# 新闻 RSS 抓取
feedparser>=6.0.0

# 读取 .env 配置文件
python-dotenv>=1.0.0

# 新闻标题中文翻译
deep-translator>=1.11.0

# HTML 截图推送图片
playwright>=1.40.0

# 定时调度（使用 --schedule 模式时需要）
schedule>=1.2.0

# PNG 截图转 PDF（自动推送 PDF 文件到企微群）
img2pdf>=0.4.0

# 图片处理（处理 Playwright 截图 RGBA 透明通道）
Pillow>=10.0.0
